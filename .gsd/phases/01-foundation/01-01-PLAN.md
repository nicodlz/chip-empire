---
phase: 1
plan: 01
wave: 1
depends_on: []
autonomous: true
---

# Plan 01-01: Project Setup

## Objective

Initialize Chip Empire with Vite, React 19, TypeScript, Tailwind CSS, Zustand, and break_eternity.js. Dark theme foundation.

## Tasks

<task type="auto">
  <name>Create Vite project</name>
  <files>package.json, vite.config.ts, tsconfig.json</files>
  <action>
    ```bash
    cd ~/Projects/idle-miner
    npm create vite@latest . -- --template react-swc-ts
    ```
    Accept overwrite if prompted.
  </action>
  <verify>npm run dev starts</verify>
  <done>Vite dev server on localhost:5173</done>
</task>

<task type="auto">
  <name>Install dependencies</name>
  <files>package.json</files>
  <action>
    ```bash
    npm install zustand break_eternity.js framer-motion lucide-react
    npm install -D @tailwindcss/vite tailwindcss
    ```
  </action>
  <verify>npm ls shows all packages</verify>
  <done>Dependencies installed</done>
</task>

<task type="auto">
  <name>Configure Tailwind + Vite</name>
  <files>vite.config.ts, src/index.css</files>
  <action>
    vite.config.ts:
    ```typescript
    import { defineConfig } from 'vite'
    import react from '@vitejs/plugin-react-swc'
    import tailwindcss from '@tailwindcss/vite'

    export default defineConfig({
      plugins: [react(), tailwindcss()],
    })
    ```
    
    src/index.css:
    ```css
    @import "tailwindcss";
    
    :root {
      --neon-blue: #00d4ff;
      --neon-green: #00ff88;
      --neon-purple: #b388ff;
    }
    ```
    
    Delete App.css.
  </action>
  <verify>Tailwind classes work</verify>
  <done>bg-slate-900 renders dark</done>
</task>

<task type="auto">
  <name>Create folder structure</name>
  <files>src/</files>
  <action>
    ```
    src/
    ├── components/
    ├── engine/
    ├── store/
    ├── hooks/
    ├── types/
    ├── data/          # Game data (minerals, chips, research)
    ├── utils/
    ├── App.tsx
    ├── main.tsx
    └── index.css
    ```
    Create empty index.ts in each folder.
  </action>
  <verify>Folders exist</verify>
  <done>Structure ready</done>
</task>

<task type="auto">
  <name>Create base App shell</name>
  <files>src/App.tsx</files>
  <action>
    ```tsx
    export default function App() {
      return (
        <div className="min-h-screen bg-gradient-to-b from-slate-950 via-slate-900 to-slate-950 text-white">
          <header className="p-4 border-b border-slate-800">
            <h1 className="text-xl font-bold text-center">
              <span className="text-[--neon-blue]">⚡</span> Chip Empire
            </h1>
          </header>
          <main className="p-4 flex flex-col items-center justify-center min-h-[80vh]">
            <div className="text-6xl mb-4">🪨</div>
            <p className="text-slate-400">Tap to mine Silicon</p>
          </main>
        </div>
      )
    }
    ```
  </action>
  <verify>App renders with dark theme</verify>
  <done>Dark theme + neon accent visible</done>
</task>

## Must-Haves

- [ ] npm run dev works
- [ ] Tailwind CSS functional
- [ ] Dark theme with neon CSS vars
- [ ] Clean folder structure
- [ ] App shows "Chip Empire" branding
